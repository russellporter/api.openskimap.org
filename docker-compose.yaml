services:
  app:
    build:
      context: .
      target: development
    environment:
      - POSTGRES_HOST=postgres
      - POSTGRES_USER=openskimap
      - POSTGRES_PASSWORD=openskimap_dev
      - POSTGRES_DB=openskimap
    volumes:
      - .:/app
    ports:
      - 3000:3000
      - 9222:9222
    command: npm run dev
    networks:
      - db
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 5s
      timeout: 3s
      retries: 10
      start_period: 30s

  postgres:
    image: postgres:18-alpine
    ports:
      - 5433:5432
    environment:
      - POSTGRES_USER=openskimap
      - POSTGRES_PASSWORD=openskimap_dev
      - POSTGRES_DB=openskimap
    volumes:
      - postgres_data:/var/lib/postgresql
    networks:
      - db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openskimap"]
      interval: 5s
      timeout: 5s
      retries: 5

networks:
  db:

volumes:
  postgres_data:
